#ifndef CGRAPH_CVALTYPE_H
#define CGRAPH_CVALTYPE_H

#include "TBasicDefine.hpp"
#include "TStatus.hpp"
#include "TException.hpp"

using TChar = mrn::TCHAR;
using TUint = mrn::TUINT;
using TSize = mrn::TSIZE;
using TVoid = mrn::TVOID;
using TVoidPtr = mrn::TVOID*;
using TInt = mrn::TINT;
using TLong = mrn::TLONG;
using TULong = mrn::TULONG;
using TBool = mrn::TBOOL;
using TIndex = mrn::TINT;
using TFloat = mrn::TFLOAT;
using TDouble = mrn::TDOUBLE;
using TConStr = mrn::TCONSTR;
using TBigBool = mrn::TBIGBOOL;

using TLevel = mrn::TINT;
using TSec = mrn::TLONG;
using TMSec = mrn::TLONG;
using TFMSec = mrn::TDOUBLE;

using TStatus = mrn::TSTATUS;
using TException = mrn::TEXCEPTION;

#endif // TVALTYPE_HPP
